# Sample environment variables for local development and deployment
# Copy to .env and fill values (never commit secrets)

# Postgres connection string from Neon (or other provider)
DATABASE_URL=postgres://user:password@host:5432/dbname

# Port used by the server (Render will provide its own PORT)
PORT=3000

# NODE_ENV=production
